<Window x:Class="FitnessNutritionTracker.EditGoalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Goal" Height="180" Width="300" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Enter new value:" FontSize="14" Margin="0,0,0,10"/>
        <TextBox x:Name="ValueTextBox" Height="30" Grid.Row="1"/>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Width="70" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Save" Width="70" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
